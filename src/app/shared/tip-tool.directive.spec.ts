import { TipToolDirective } from './tip-tool.directive';

describe('TipToolDirective', () => {
  it('should create an instance', () => {
    const directive = new TipToolDirective();
    expect(directive).toBeTruthy();
  });
});
